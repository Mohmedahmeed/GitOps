pipeline {
    agent any

    stages {
        stage('deploy') {
                steps {
                    // Run the docker-compose
                    cd 'cd/docker/'
                    sh 'docker-compose up'
                }
            }
    }
}