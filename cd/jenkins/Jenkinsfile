pipeline {
    agent any

    stages {
        stage('deploy') {
            dir('cd/docker/'){
                steps {
                    // Run the docker-compose
                    sh 'docker-compose up'
                }
            }
        }
    }
}