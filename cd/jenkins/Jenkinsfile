pipeline {
    agent any

    stages {
        stage('deploy') {
                steps {
                    // Run the docker-compose
                    sh 'cd cd/docker/'
                    sh 'ls'
                    sh 'pwd'
                    sh 'docker-compose up'
                }
            }
    }
}